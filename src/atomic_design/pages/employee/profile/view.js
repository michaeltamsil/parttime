import React, { Component } from 'react';
import { connect } from 'react-redux';
import { NavLink } from 'react-router-dom';

class Index extends Component {
  
  render = () => {
    return (<div> ini profile
    </div>);
  }
}
export default connect()(Index);
